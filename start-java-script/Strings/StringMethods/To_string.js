const text = 12984;

const res = typeof(text);   // number
console.log(`Line${4}: `, res);

const newText  = text.toString()
const res2 = typeof(newText);   // string
console.log(`Line${8}: `, res2);

